import { createAction } from '@ngrx/store';

export const increment = createAction('[Datatable Component] Increment');
export const decrement = createAction('[Datatable Component] Decrement');
export const reset = createAction('[Datatable Component] Reset');
